#!/usr/bin/env bash
# Stub for pacman's vercmp: prints -1, 0, or 1.
if [[ "$1" == "$2" ]]; then
    echo 0
elif [[ "$(printf '%s\n%s\n' "$1" "$2" | sort -V | head -n 1)" == "$1" ]]; then
    echo -1
else
    echo 1
fi
